{
  "name": "extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node esbuild.js --development",
    "build": "tsc && node esbuild.js"
  },
  "dependencies": {
    "shared-lib": "^0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "esbuild-plugin-copy": "^2.1.1",
    "typescript": "^5"
  }
}
